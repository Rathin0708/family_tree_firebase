import 'package:flutter/material.dart';

void main() {
  runApp(const Family_tree());
}
class Family_tree extends StatelessWidget {
  const Family_tree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
