rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for testing - will lock down later
    match /{document=**} {
      allow read, write: if true;
    }
    
    // This is a temporary rule to allow all access during development
    // In production, replace with proper security rules
    // match /users/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }
    
    // match /families/{familyId} {
    //   allow read, write: if request.auth != null;
    // }
    
    // match /stories/{storyId} {
    //   allow read: if request.auth != null;
    //   allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    //   allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    // }
    
    // match /stories/{storyId}/comments/{commentId} {
    //   allow read: if request.auth != null;
    //   allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    //   allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    // }
  }
}
